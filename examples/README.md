# Example inputs

The public repository does not distribute clinical images, labels, or hospital model weights.
Use a locally authorized video and provide a segmentation callable with the `module:function`
interface described in the root README. The Streamlit dashboard uses a clearly labeled synthetic
sequence so the temporal controller can be inspected without protected health data.

